
// Creates a nCent wallet for a user. Returns the private/public key of the 
// generated wallet.

const createWallet = (privateKey, publicKey) => {
    // gnerate Wallet first
    // assign public Key and private Key to the user
    // return all of this
}